{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the HUD window receive the events the backend emits. Without a capability file Tauri v2 grants nothing, so window.__TAURI__.event.listen is denied and every audio.level, stt.final and answer.delta event is dropped before it reaches the UI - which looks exactly like a dead capture pipeline even when capture is working.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
